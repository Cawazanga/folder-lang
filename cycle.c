#include "func.h"
#include <stdio.h>
int cycle(char numsbuf, short count) {
    puts("db: cyclu");
    if (numsbuf == '1') {
        puts("db: 1");
        for (int i = count; i != 0; i--) {
            runoldbuf(sbuf1, iv);
        }
    }
    if (numsbuf == '2') {
        puts("db: 2");
        for (int i = count; i != 0; i--) {
            runoldbuf(sbuf2, iv);
        }
    }

}
